name: cors_api
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.2 <3.0.0'

dependencies:
  shelf_router: ^1.1.2
  path: ^1.8.0
  shelf_plus: ^1.0.1
  http:
  shelf_cors_headers: ^0.1.2

dev_dependencies:
  lints: ^1.0.0

